/* Astro Fighters World Package 03 — Imperial stone family v2
   Reference-gate-passed Phaser integration candidate.
   No checklist completion is implied by this bridge. */
(() => {
  "use strict";
  const MANIFEST_URL = "assets/metadata/world/imperial-stone-family-package03.json";
  const TEXTURE_KEY = "af-world-p03-stone-family";
  const API = window.AF_WORLD_PACKAGE03 = window.AF_WORLD_PACKAGE03 || {};
  API.version = "package03-v2";
  API.status = "reference-gate-passed-integration-candidate";
  function notice(message){console.warn("[AF world p03]",message);let el=document.getElementById("af-world-package03-warning");if(!el){el=document.createElement("div");el.id="af-world-package03-warning";el.style.cssText="position:fixed;z-index:99998;left:8px;bottom:8px;max-width:500px;padding:8px 10px;background:#141410;color:#F5D5BB;border:1px solid #756A55;font:11px/1.35 ui-monospace,monospace;pointer-events:none";document.body.appendChild(el)}el.textContent=message}
  function findScene(){const P=window.Phaser;if(!P||!Array.isArray(P.GAMES)||!P.GAMES.length)return null;const game=P.GAMES.find(Boolean);const scene=game?.scene?.getScenes(true)?.find(s=>s?.sys?.isActive());return scene?{P,game,scene}:null}
  function locatePlayer(scene){const candidates=[scene.player,scene.registry?.get?.("player"),scene.data?.get?.("player")];for(const o of scene.children?.list||[]){if(!o)continue;const n=String(o.name||"").toLowerCase();if(["player","fighter","hero"].includes(n))candidates.push(o);if(typeof o.getData==="function"&&(o.getData("isPlayer")||o.getData("playerControlled")))candidates.push(o)}return candidates.find(o=>o&&Number.isFinite(o.x)&&Number.isFinite(o.y))||null}
  async function fetchManifest(){const r=await fetch(MANIFEST_URL,{cache:"no-store"});if(!r.ok)throw new Error("manifest "+r.status);return r.json()}
  function registerFrames(scene,manifest){const texture=scene.textures.get(TEXTURE_KEY);if(!texture||texture.key==="__MISSING")throw new Error("stone family atlas texture missing");for(const [name,rect] of Object.entries(manifest.runtime.frames)){if(!texture.has(name))texture.add(name,0,rect[0],rect[1],rect[2],rect[3])}texture.setFilter(Phaser.Textures.FilterMode.NEAREST)}
  function loadTexture(scene,manifest){return new Promise((resolve,reject)=>{if(scene.textures.exists(TEXTURE_KEY)){registerFrames(scene,manifest);return resolve()}const fail=file=>reject(new Error("stone family atlas load failed: "+(file?.src||"unknown")));scene.load.once("loaderror",fail);scene.load.once("complete",()=>{scene.load.off("loaderror",fail);try{registerFrames(scene,manifest);resolve()}catch(e){reject(e)}});scene.load.image(TEXTURE_KEY,manifest.runtime.atlas_image);if(!scene.load.isLoading())scene.load.start()})}
  function hash(cx,cy,salt=0){let h=Math.imul((cx+salt)|0,73856093)^Math.imul((cy-salt)|0,19349663);h^=h>>>13;h=Math.imul(h,1274126177);h^=h>>>16;return Math.abs(h|0)}
  function createGround(scene,manifest){const [stepX,stepY]=manifest.integration.macro_step;const target=locatePlayer(scene);const container=scene.add.container(0,0);container.name="af-world-package03-stone-family";const radiusX=3,radiusY=3;const materials=manifest.runtime.materials.slice();const modes=["clean","cracked","patched","damp","mixed"];const cells=[];for(let iy=-radiusY;iy<=radiusY;iy++){for(let ix=-radiusX;ix<=radiusX;ix++){const image=scene.add.image(0,0,TEXTURE_KEY,"clean-0").setOrigin(0,0);image.name=`af-world-p03-${ix}-${iy}`;cells.push({ix,iy,image});container.add(image)}}const state={scene,manifest,target,container,cells,enabled:true,materialMode:"clean",cellX:NaN,cellY:NaN};function centerPoint(){if(state.target&&state.target.active!==false)return{x:state.target.x,y:state.target.y};const cam=scene.cameras?.main;return cam?{x:cam.worldView.centerX,y:cam.worldView.centerY}:{x:0,y:0}}function materialFor(gx,gy){if(state.materialMode!=="mixed")return state.materialMode;const h=hash(gx,gy,71)%100;if(h<67)return"clean";if(h<79)return"cracked";if(h<90)return"patched";return"damp"}function refresh(force=false){const p=centerPoint();const cx=Math.floor(p.x/stepX),cy=Math.floor(p.y/stepY);if(!force&&cx===state.cellX&&cy===state.cellY)return;state.cellX=cx;state.cellY=cy;for(const c of cells){const gx=cx+c.ix,gy=cy+c.iy;const mat=materialFor(gx,gy);const variant=hash(gx,gy,materials.indexOf(mat)*17+3)&3;c.image.setPosition(gx*stepX,gy*stepY);c.image.setFrame(`${mat}-${variant}`)}}state.setVisible=visible=>{state.enabled=!!visible;container.setVisible(state.enabled)};state.setMaterial=material=>{if(!modes.includes(material))return false;state.materialMode=material;state.cellX=NaN;state.cellY=NaN;refresh(true);return true};state.cycleMaterial=()=>{const i=modes.indexOf(state.materialMode);state.setMaterial(modes[(i+1)%modes.length]);notice(`Package 03 ground mode: ${state.materialMode}. F6 cycles material; F7 toggles ground.`)};state.destroy=()=>container.destroy(true);refresh(true);const keys=scene.input?.keyboard?.addKeys({cycle:"F6",toggle:"F7"})||null;const update=()=>{if(!container.active)return;refresh(false);container.setDepth(Number.isFinite(state.target?.depth)?state.target.depth-100:-10);if(keys&&Phaser.Input.Keyboard.JustDown(keys.cycle))state.cycleMaterial();if(keys&&Phaser.Input.Keyboard.JustDown(keys.toggle))state.setVisible(!state.enabled)};scene.events.on("update",update);scene.events.once("shutdown",()=>scene.events.off("update",update));API.active=state;return state}
  API.install=async function install(){try{const found=findScene();if(!found){notice("World Package 03: active Phaser scene not detected. Integration did not attach.");return null}const manifest=await fetchManifest();await loadTexture(found.scene,manifest);const state=createGround(found.scene,manifest);notice("World Package 03 v2 attached in Phaser. F6 cycles clean/cracked/patched/damp/mixed; F7 toggles. Hidden 32×32 grid lines are never rendered.");return state}catch(e){notice("World Package 03 integration failed: "+(e?.message||e));return null}};
  function boot(attempt=0){if(findScene())return API.install();if(attempt<80)return setTimeout(()=>boot(attempt+1),250);notice("World Package 03: Phaser runtime was not detected after 20 seconds.")}boot();
})();
