# Slice 0 stone-clean candidate v1

This directory contains source-authoring candidates for `AF-IC-SHARED-FOUNDATION`.

`stone-clean.png` is **not source-approved**. It exists so visual review and iteration can happen against the real contracted 1920×1280 source master rather than a presentation-board proxy.

Machine state remains disabled until the source/reference gate is passed.
